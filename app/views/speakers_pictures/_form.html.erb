<%= simple_form_for(@speakers_picture) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :speaker_image %>
    <%= f.input :speaker_id %>
    <%= f.input :image_processed %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
