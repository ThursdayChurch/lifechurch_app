<%= simple_form_for(@sermons_picture) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :sermon_image %>
    <%= f.input :image_processed %>
    <%= f.input :sermons_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
