<%= simple_form_for(@ministry_children_picture) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :ministrychild_image %>
    <%= f.input :image_processed %>
    <%= f.input :ministrychild_id %>
    <%= f.input :ministry_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
