<%- model_class = MinistryChildrenPicture -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:ministrychild_image) %></th>
      <th><%= model_class.human_attribute_name(:image_processed) %></th>
      <th><%= model_class.human_attribute_name(:ministrychild_id) %></th>
      <th><%= model_class.human_attribute_name(:ministry_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @ministry_children_pictures.each do |ministry_children_picture| %>
      <tr>
        <td><%= link_to ministry_children_picture.id, ministry_children_picture_path(ministry_children_picture) %></td>
        <td><%= ministry_children_picture.ministrychild_image %></td>
        <td><%= ministry_children_picture.image_processed %></td>
        <td><%= ministry_children_picture.ministrychild_id %></td>
        <td><%= ministry_children_picture.ministry_id %></td>
        <td><%=l ministry_children_picture.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_ministry_children_picture_path(ministry_children_picture), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      ministry_children_picture_path(ministry_children_picture),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_ministry_children_picture_path,
            :class => 'btn btn-primary' %>
